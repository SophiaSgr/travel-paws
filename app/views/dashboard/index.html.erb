<div class="container-profile d-flex align-items-center">
  <div>
    <h1 class="dash-h lato bold title-placement">Your Dashboard</h1>
    <p>
      <%= cl_image_tag current_user.avatar.key, class:
      "avatar-profile-dashboard" %>
    </p>
  </div>
  <div class="position-user-name">
    <h6></h6>
    <p><%= current_user.first_name%></p>
    <p><%= current_user.last_name%></p>
  </div>
  <div>
    <p><%= current_user.bio%></p>
  </div>
</div>





<div class="format lato">

  <div class="div-tabs">
    <div class="wrapper-tabs">
      <input class="radio" id="one" name="group" type="radio" checked />
      <input class="radio" id="two" name="group" type="radio" />
      <input class="radio" id="three" name="group" type="radio" />
      <div class="tabs">
<div>
</div>
        <div class="position-tabs">
          <% if current_user.is_shelter_owner? %>
          <label class="tab tab-boxshadow-right" id="one-tab" for="one"
            >Outgoing Requests</label
          >
          <label class="tab tab-boxshadow-right" id="two-tab" for="two"
            >Incoming Shelter</label
          >
           <label class="tab tab-boxshadow-right" id="three-tab" for="three"
            >My Shelters</label
          >
            <% else %>
          <label class="tab tab-boxshadow-right" id="one-tab" for="one"
            >Outgoing Requests</label
          >
          <% end %>
        </div>
       </div>
      <div class="panels">
        <div class="panel" id="one-panel">
          <div class="panel-title">Become a Guardian</div>
          <div>
            <% @outgoing_requests.each do |request| %>
            <div class="notification">
              <%= cl_image_tag current_user.avatar.key, class: "avatar-large" %>
                <div class="notification-content">
                  <p><small><%= request.created_at %></small></p>

                  <p>Lorem ipsum dolor sit amet, <strong>consectetur</strong> adipisicing elit.</p>
                </div>

                <div class="notification-actions">
                  <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
                  <a href="#">Delete <i class="far fa-trash-alt"></i></a>
                </div>
              </div>
            </div>

            <h6>Why I want to become a Guardian:</h6>
              <%= request.message %>
              <br>
              <h6>Request Status</h6>
                <%= request.status %>
            <% end %>
          </div>
        </div>
        <div class="panel" id="two-panel">
          <div class="panel-title">Your Requests</div>
          <p><% if current_user.is_shelter_owner? %></p>
          <p>
            <% @incoming_requests.each do |r| %>
            <div class="styling-field">
              <h6>Guardian Message:</h6>
              <%= r.message %>
            </div>
            <div class="styling-field">
              <h6>Status</h6>
              <%= r.status %>
            </div>
            <% if r.pending? %>
              <%= link_to "Accept", accept_path(r), method: :patch, controller: "requests_controller", class: "sec-btn-status" %>
              <%= link_to "Decline", decline_path(r), method: :patch, controller: "requests_controller", class: "sec-btn-status" %>
            <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="panel" id="three-panel">
          <p><% if current_user.is_shelter_owner? %></p>
          <div class="panel-title">Your Shelters</div>
          <br>
          <% @shelters.each do |shelter| %>
          <ul>
            <li><%= shelter.name %></li>
            <li>information</li>
          </ul>
          <% end %>
          <% end %>
        </div>
    </div>
  </div>
</div>


